const routes = {
  home: "/",
  productsDetail: "/product-detail/:id",
  products: "/products",
  admin: "/admin",
  login: "/login",
  stored: "/stored/featured-products",
};

export default routes;
